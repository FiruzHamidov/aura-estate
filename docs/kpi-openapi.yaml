openapi: 3.0.3
info:
  title: Aura KPI API
  version: 1.2.0
servers:
  - url: /api
security:
  - bearerAuth: []
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: Token
  schemas:
    KpiRole:
      type: string
      enum: [agent, intern, mop, rop]
    KpiError:
      type: object
      required: [code, message, details, trace_id]
      properties:
        code: { type: string }
        message: { type: string }
        details:
          type: object
          properties:
            errors:
              type: object
              additionalProperties:
                type: array
                items: { type: string }
        trace_id: { type: string }
    KpiPlanReadItem:
      type: object
      required: [metric_key, daily_plan, weight, comment, effective_from, effective_to]
      properties:
        id: { type: integer }
        role: { $ref: '#/components/schemas/KpiRole' }
        metric: { type: string, description: Legacy alias of metric_key }
        metric_key: { type: string }
        daily_plan: { type: number }
        weight: { type: number }
        comment: { type: string }
        effective_from: { type: string, format: date, nullable: true }
        effective_to: { type: string, format: date, nullable: true }
        updated_at: { type: string, format: date-time, nullable: true }
        branch_id: { type: integer, nullable: true }
        branch_group_id: { type: integer, nullable: true }
        user_id: { type: integer, nullable: true }
        source: { type: string, enum: [personal, common] }
        plan_source: { type: string, enum: [personal, common] }
    KpiPlanItem:
      type: object
      properties:
        metric: { type: string }
        metric_key: { type: string }
        daily_plan: { type: number }
        weight: { type: number }
        comment: { type: string }
    KpiBulkResult:
      type: object
      properties:
        user_id: { type: integer }
        ok: { type: boolean }
        code: { type: string }
        message: { type: string }
        details:
          type: object
          properties:
            row: { type: integer }
            role: { $ref: '#/components/schemas/KpiRole' }
            errors:
              type: object
paths:
  /kpi/plans/bulk-upsert:
    post:
      summary: Bulk upsert personal KPI plans
      description: |
        Supports role multiselect in scope. Priority is `scope.roles[]` > `scope.target_roles[]` > `scope.role`.
        If only `scope.role` is sent, backward compatibility is preserved.
        Response always contains partial success results.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [effective_from, scope, rows]
              properties:
                effective_from: { type: string, format: date }
                effective_to: { type: string, format: date, nullable: true }
                scope:
                  type: object
                  properties:
                    role: { $ref: '#/components/schemas/KpiRole' }
                    roles:
                      type: array
                      items: { $ref: '#/components/schemas/KpiRole' }
                    target_roles:
                      type: array
                      items: { $ref: '#/components/schemas/KpiRole' }
                    branch_id: { type: integer }
                    branch_group_id: { type: integer }
                rows:
                  type: array
                  items:
                    type: object
                    required: [user_id, items]
                    properties:
                      user_id: { type: integer }
                      items:
                        type: array
                        items:
                          allOf:
                            - $ref: '#/components/schemas/KpiPlanItem'
                          anyOf:
                            - required: [metric_key]
                            - required: [metric]
      responses:
        '200':
          description: Partial success result
          content:
            application/json:
              schema:
                type: object
                properties:
                  success_count: { type: integer }
                  failed_count: { type: integer }
                  results:
                    type: array
                    items: { $ref: '#/components/schemas/KpiBulkResult' }
              examples:
                mixed_result:
                  value:
                    success_count: 1
                    failed_count: 2
                    results:
                      - user_id: 101
                        ok: true
                      - user_id: 102
                        ok: false
                        code: KPI_VALIDATION_FAILED
                        message: Validation failed.
                        details:
                          row: 1
                          role: intern
                          errors:
                            items:
                              - The sum of all items.weight must be exactly 1.0.
                      - user_id: 103
                        ok: false
                        code: KPI_FORBIDDEN_SCOPE
                        message: Forbidden in current scope.
                        details:
                          row: 2
                          role: rop
                          errors: {}
        '403':
          description: Forbidden
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KpiError' }
        '409':
          description: Period conflict
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KpiError' }
        '422':
          description: Validation failed
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KpiError' }
  /kpi/plans/common:
    get:
      summary: Read common KPI plans by one or many roles
      description: Returns flat array of plans with mandatory `role` field on each row.
      parameters:
        - in: query
          name: role
          schema: { $ref: '#/components/schemas/KpiRole' }
          required: false
        - in: query
          name: roles[]
          schema:
            type: array
            items: { $ref: '#/components/schemas/KpiRole' }
          style: form
          explode: true
          required: false
        - in: query
          name: role_in[]
          schema:
            type: array
            items: { $ref: '#/components/schemas/KpiRole' }
          style: form
          explode: true
          required: false
        - in: query
          name: date
          schema: { type: string, format: date }
          required: true
        - in: query
          name: branch_id
          schema: { type: integer }
        - in: query
          name: branch_group_id
          schema: { type: integer }
      responses:
        '200':
          description: Flat list with role field
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/KpiPlanReadItem' }
                  plans:
                    type: array
                    items: { $ref: '#/components/schemas/KpiPlanReadItem' }
                  meta:
                    type: object
                    required: [exists, source]
                    properties:
                      exists: { type: boolean }
                      source: { type: string, enum: [common] }
        '403':
          description: Forbidden
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KpiError' }
        '422':
          description: Validation failed
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KpiError' }
    put:
      summary: Upsert common KPI plans
      responses:
        '200': { description: OK }
        '403':
          description: Forbidden role action (e.g. MOP cannot edit common plans)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KpiError' }
    patch:
      summary: Upsert common KPI plans
      responses:
        '200': { description: OK }
        '403':
          description: Forbidden role action (e.g. MOP cannot edit common plans)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KpiError' }
  /kpi-plans:
    get: { summary: List KPI plans, responses: { '200': { description: OK } } }
    patch: { summary: Update KPI plans, responses: { '200': { description: OK }, '403': { description: Forbidden } } }
  /kpi/plans:
    get:
      summary: Read effective personal KPI plans for user by date
      parameters:
        - in: query
          name: user_id
          schema: { type: integer }
          required: true
        - in: query
          name: date
          schema: { type: string, format: date }
          required: false
      responses:
        '200':
          description: Effective plans (personal preferred over common)
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/KpiPlanReadItem' }
                  plans:
                    type: array
                    items: { $ref: '#/components/schemas/KpiPlanReadItem' }
                  source:
                    type: string
                    nullable: true
                    enum: [personal, common, null]
                  meta:
                    type: object
                    required: [exists, source]
                    properties:
                      exists: { type: boolean }
                      source:
                        type: string
                        nullable: true
                        enum: [personal, common, null]
        '401':
          description: Unauthenticated
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KpiError' }
        '403':
          description: Forbidden in current scope
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KpiError' }
        '422':
          description: Validation failed
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KpiError' }
  /kpi/plans/{userId}:
    patch:
      summary: Upsert personal KPI plans for one user
      parameters:
        - in: path
          name: userId
          required: true
          schema: { type: integer }
      responses:
        '200': { description: OK }
        '401': { description: Unauthenticated }
        '403': { description: Forbidden in current scope / role action }
        '409': { description: Plan period conflict }
        '422': { description: Validation failed }
    put:
      summary: Upsert personal KPI plans for one user
      parameters:
        - in: path
          name: userId
          required: true
          schema: { type: integer }
      responses:
        '200': { description: OK }
        '401': { description: Unauthenticated }
        '403': { description: Forbidden in current scope / role action }
        '409': { description: Plan period conflict }
        '422': { description: Validation failed }
  /kpi/daily:
    get: { summary: Daily KPI rows, responses: { '200': { description: OK } } }
    post: { summary: Save daily KPI, responses: { '201': { description: Created }, '422': { description: Validation error } } }
  /kpi/weekly:
    get: { summary: Weekly KPI rows, responses: { '200': { description: OK } } }
  /kpi/monthly:
    get: { summary: Monthly KPI rows, responses: { '200': { description: OK } } }
  /kpi/metric-mapping:
    get: { summary: KPI metric mapping dictionary (v2), responses: { '200': { description: OK } } }
  /kpi/dashboard:
    get: { summary: KPI dashboard summary and ranking, responses: { '200': { description: OK } } }
  /kpi/integrations/status:
    get: { summary: KPI integration statuses, responses: { '200': { description: OK } } }
  /kpi/telegram-reports/config:
    get: { summary: Telegram report config, responses: { '200': { description: OK } } }
    patch: { summary: Update telegram report config, responses: { '200': { description: OK } } }
  /kpi/quality/issues:
    get: { summary: KPI quality issues, responses: { '200': { description: OK } } }
  /kpi/early-risk-alerts:
    get: { summary: Early risk alerts, responses: { '200': { description: OK } } }
  /kpi/early-risk-alerts/status:
    patch: { summary: Update early risk alert status, responses: { '200': { description: OK } } }
  /kpi/period-contract:
    get: { summary: KPI period contract rows, responses: { '200': { description: OK } } }
  /kpi/acceptance-runs:
    get: { summary: Acceptance run logs, responses: { '200': { description: OK } } }
  /kpi/adjustments:
    get: { summary: KPI adjustments list, responses: { '200': { description: OK } } }
    post: { summary: Create KPI adjustment, responses: { '201': { description: Created } } }
  /crm/tasks/kpi-daily-summary:
    get: { summary: CRM task KPI daily summary, responses: { '200': { description: OK } } }
  /crm/tasks/kpi-weekly-summary:
    get: { summary: CRM task KPI weekly summary, responses: { '200': { description: OK } } }
