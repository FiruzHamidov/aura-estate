# Include inside the application's server block before serving public storage.
# Authorized downloads use /api/external-request-photos/{id}.
location ^~ /storage/external-property-requests/ {
    return 404;
}
